# Dependabot: hält npm-Abhängigkeiten und die GitHub-Actions-Versionen aktuell.
#
# Lauf alle zwei Wochen: am 1. und 15. um 06:00 Europe/Berlin (Standard-Cron kennt
# kein echtes "alle 14 Tage", 1./15. ist die übliche zweiwöchentliche Näherung).
#
# cooldown.default-days: 14  ->  Dependabot schlägt eine Version erst vor, wenn sie
# mindestens zwei Wochen veröffentlicht ist. Damit landet nie ein Framework-Update
# (next, react, tailwindcss ...) jünger als zwei Wochen im PR; frisch veröffentlichte,
# evtl. fehlerhafte Releases reifen erst.
version: 2
updates:
  - package-ecosystem: 'npm'
    directory: '/'
    schedule:
      interval: 'cron'
      cron: '0 6 1,15 * *'
      timezone: 'Europe/Berlin'
    cooldown:
      default-days: 14
    open-pull-requests-limit: 5
    commit-message:
      prefix: 'chore(deps)'
      prefix-development: 'chore(deps-dev)'
    groups:
      # Weniger PR-Rauschen: zusammengehörige Pakete in je einem PR bündeln.
      react:
        patterns:
          - 'react'
          - 'react-dom'
          - '@types/react'
          - '@types/react-dom'
      next:
        patterns:
          - 'next'
          - 'eslint-config-next'
          - '@opennextjs/*'
      tailwind:
        patterns:
          - 'tailwindcss'
          - '@tailwindcss/*'

  - package-ecosystem: 'github-actions'
    directory: '/'
    schedule:
      interval: 'cron'
      cron: '0 6 1,15 * *'
      timezone: 'Europe/Berlin'
    cooldown:
      default-days: 14
    commit-message:
      prefix: 'ci(actions)'
